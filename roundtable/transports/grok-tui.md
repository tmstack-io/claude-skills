# grok TUI 輸送路 — 固有差分

grok の対話ハーネスを賢者ペインで起用するときのハーネス固有差分。共通手順の正本は [../tui-protocol.md](../tui-protocol.md)（ペイン操作は `mux.sh` 経由）。

## 起動

`mux.sh run` に渡す起動コマンド:

```sh
grok --cwd '<プロジェクトルート>'
```

- モデルは当該席の `--model` 指定時のみ `-m <モデル>` を付加する。省略時はアカウントの既定（入力欄フッターに表示される。編成の宣言・question.md に記載する）。

## trust ダイアログ

未 trust のディレクトリでは codex と同型の trust ダイアログが表示される（0.2.114 実測。cwd 単位）。`mux.sh wait-output <ペインID> "Do you trust" 15000` で確定待ちし、マッチしたら `mux.sh key <ペインID> y` の**単キー送信**（Enter 不要）で通過する（タイムアウト＝ダイアログなし・trust 済みは正常分岐）。

## エージェント検知と受理判定

- エージェント名 `grok`。**`agent_session` は null のことがある**（実測）— 受理完了の判定に `agent_session` は使わず、working 遷移＋（未遷移時は）tui-protocol 手順3の二分で判定する。
- 起動直後の `agent_not_found` レース（tui-protocol「ペインの起動」手順5の再実行で対処）は grok で実測した事象である。

## 権限モデル

権限は**選択的承認モデル**である（0.2.114 実測）。読み取り系のシェル実行・ファイルの読み書き・通信規約の push は無確認で実行されるが、**書き込み効果のあるシェルコマンド（`mkdir` 等）は承認ダイアログ（`1 always-approve ／ 2 Yes, proceed ／ 3 No, reject`）を出し、エージェント状態が `blocked` になる**（ペインタイトルに「⚠ Action Required」）。裁定は tui-protocol の安全網発火時三分類の blocked 分岐として行う: `mux.sh read`（--scrollback）で内容を確認し、妥当なら `mux.sh send <ペインID> "2"` ＋ `mux.sh key <ペインID> Enter`（今回のみ許可）で通過し、安全網を張り直す。`1`（always-approve）は書き込みスコープを別途制限できる場合に限る。承認が出ない書き込みも多いため、**書き込みの遮断はブリーフの規律が第一の防御**である。

## 固有の注意

- **`mux.sh read` は `--scrollback` を必ず付ける**: grok は terminal alternate screen で描画するため、可視画面ソースでは応答本文が見えないことがある（実測）。受理判定の二分・応答の直接回収・ダイアログ確認はすべて `--scrollback` で行う。
- ツール活動（`◈ Read` / `◆ Creating ...`）と思考の経過（`◆ Thought for ...`）がペインに表示され、観戦できる。
- 賢者自身による push は無確認で実行され、確実に届く（実測: 2回の合議で計6/6到達。選択的承認モデルでも push はブロックされなかった）。pull 安全網は共通原則どおり併設する。

## 検証記録

2026-07-30 実測（検証＋合議1件・3ラウンドの実運用）: trust ダイアログなしの起動（trust 済み cwd）/ エージェント検知（`grok`・status。`agent_session` は null）/ 読み取りの無確認実行 / ワークスペース内書き込みの無確認実行 / push の無確認実行と到達（3/3。書き込み規律・ブリーフ遵守も3ラウンドとも違反なし）/ 委譲の受理と同一ペイン追送によるラウンド間文脈保持 / モデル名のフッター表示（`Grok 4.5 (high)`）/ alt-screen 描画により可視ソースで応答が見えない事象と scrollback での回収。

2026-07-30 追試（同日中の別プロジェクト cwd での合議1件・3ラウンドの実運用、grok 0.2.114）: trust ダイアログの表示確認（cwd 単位。`y` 単キーで通過。同日先行実測の「表示されない」は trust 済みディレクトリでの観測とみなす）/ 書き込み効果シェルでの承認ダイアログと blocked 遷移（安全網の発火で検知し `2`＋Enter で通過）/ 読み取り系シェル・ファイル書き込み・push の無確認実行（push 3/3 到達）/ 起動直後の `agent_not_found` レースと再実行での解消。

未検証: `--allow` / `--deny` ルールによる書き込みスコープの宣言的制限（規律より強い遮断が欲しくなった場合の改善候補）。
